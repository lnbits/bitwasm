# Test Vectors

This directory will hold canonical vectors for receipt encoding, receipt hashing,
VM execution, and container commitments.

Directories:
- `receipts/`: canonical receipt encodings + hashes
- `vm/`: WASM execution vectors (state/input/outputs/gas)
- `containers/`: container commitment examples
